window.aboutcomponent = {
    // This component only needs a template, so it stays very small.
        route: '/about',
        template: /* html */ `
        <section>
            <h1>About</h1>
            <p>This is a tiny SPA!</p>
        </section>
        `
};